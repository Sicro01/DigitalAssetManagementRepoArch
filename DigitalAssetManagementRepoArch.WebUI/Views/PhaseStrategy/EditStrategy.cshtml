@model DigitalAssetManagementRepoArch.Application.Strategies.Queries.GetStrategy.GetStrategyByIdViewModel

<div class="row">
    <div class="col-11">
        <h2 class="text-dark">Edit Strategy</h2>
    </div>
</div>
<br />
<div class="">
    <form method="post">
        <div asp-validation-summary="All" class="text-danger"></div>
        @Html.HiddenFor(m => m.DisplayStrategyDto.Id)
        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th scope="col" class="text-center" style="width: 35%;">
                        <label asp-for="DisplayStrategyDto.Name"></label>
                    </th>
                    <th scope="col" class="text-center" style="width: 25%;">
                        <label asp-for="DisplayStrategyDto.StartDate"></label>
                    <th scope="col" class="text-center" style="width: 25%;">
                        <label asp-for="DisplayStrategyDto.EndDate"></label>
                    </th>
                    <th scope="col" class="text-center" style="width: 15%;">
                        <label>Manage Strategy</label>
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        <input asp-for="DisplayStrategyDto.Name" class="form-control" />
                        <span asp-validation-for="DisplayStrategyDto.Name" class="text-danger"></span>
                    </td>
                    <td>
                        <input asp-for="DisplayStrategyDto.StartDate" class="form-control" />
                        <span asp-validation-for="DisplayStrategyDto.StartDate" class="text-danger"></span>
                    </td>
                    <td>
                        <input asp-for="DisplayStrategyDto.EndDate" class="form-control" />
                        <span asp-validation-for="DisplayStrategyDto.EndDate" class="text-danger"></span>
                    </td>
                    <td class="d-flex justify-content-around">
                        <button type="submit" class="btn btn-success">Save</button>
                        <a asp-action="Cancel" type="submit" class="btn btn-secondary">Cancel</a>

                    </td>
                </tr>
            </tbody>
        </table>
    </form>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}